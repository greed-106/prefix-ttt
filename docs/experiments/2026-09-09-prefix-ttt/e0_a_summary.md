# E0 与阶段 A 执行完成总结

2026-09-10核查：Supervisor/SQLite中的三个E0任务与阶段A全部退出0，消费者于03:24结束，四张A40空闲。

## E0 官方质量基线

使用锁定的lmms-eval0.7.2原生任务、生成与评分，完整样本日志及结果位于 `artifacts/lmms/e0/`。三任务limit均为空，固定样本数、唯一完整doc_id与有限指标检查通过。

| 任务 | 样本数 | 官方指标 |
| --- | ---: | --- |
| MME | 2374 | Perception 1481.12；Cognition 341.79 |
| POPE | 9000 | Precision 93.55%；Recall 75.78%；F1 83.73% |
| GQA | 12578 | Accuracy 61.27% |

这些是原始完整LLaVA基线，不是Prefix-TTT迁移后的结果。不同量纲不合并平均。

## 阶段 A

四卡执行50000样本、391次更新；`artifacts/training/A/result.json`标记正式完整执行，manifest SHA与固定清单一致，`steps.jsonl`有391条记录。`latest.pt`为339875595字节，包含后续迁移所需训练产物；本次状态核查未额外加载它进行模型推理。SQLite计时约80.9分钟，包含进程启动与保存开销。

本阶段完成的是原Full-MSA教师下的逐层独立attention残差迁移，不是完整混合模型SFT。末步各层误差存在差异，不能仅凭任务退出成功推断替换后质量保持。

## 问题与后续

正式四项任务没有失败状态。接入预跑曾遇到model_args类型及make_table导入位置错误，针对官方API修复后四卡8样本预跑通过；不影响本次完整结果，失败证据保留在 `artifacts/lmms/`。旧自定义评测代码及结果已按用户要求删除，不纳入结论。

下一步按计划进行A后整模型切换诊断，再启动E1/E2同预算Pilot，之后沿各自完整B scheduler继续。B尚未排队；此次没有自动提交下一阶段或重跑成功任务。
