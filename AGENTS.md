# 项目协作规范

## 项目目标

本项目用于Test-Time-Training相关的研究。

## 开始工作前

1. 阅读本文件。
2. 阅读本次长程实验的稳定项目目录
   `docs/experiments/YYYY-MM-DD-<experiment-project>/` 中的主账本。
3. 查询任务调度器状态与已有运行结果，禁止重复提交已经成功的任务。
4. 检查工作树，保留其他智能体和用户的改动，不覆盖无关文件。

发生上下文压缩或恢复新会话后，也必须重新执行以上步骤。

## 通用编码行为准则

以下准则用于减少 LLM 编码中的常见错误，应根据需要与本文件的项目专用规范合并执行。

**权衡：** 这些准则更偏重谨慎而非速度。对于琐碎任务，应结合实际情况判断。

### 1. 编码前先思考

**不要凭空假设，不要掩饰困惑，主动说明取舍。**

实现前：

- 明确陈述你的假设。如果不确定，先询问。
- 如果存在多种理解，将它们列出，不得默默选择其中一种。
- 如果存在更简单的方案，明确说明。必要时对不合理的要求提出异议。
- 如果有不清楚的地方，停下来，指出具体困惑并询问。

### 2. 简单优先

**只编写解决问题所需的最少代码，不做推测性设计。**

- 不增加超出请求范围的功能。
- 不为仅使用一次的代码引入抽象。
- 不增加未被要求的“灵活性”或“可配置性”。
- 不为按现有约束不可能发生的情形编写错误处理。
- 如果写了 200 行而同一问题可以用 50 行解决，重写并简化它。

问问自己：“一位资深工程师会认为这过度复杂吗？”如果会，就继续简化。

### 3. 外科手术式修改

**只触及必须修改的部分，只清理自己造成的问题。**

编辑现有代码时：

- 不要顺手“改进”相邻的代码、注释或格式。
- 不要重构未损坏的部分。
- 遵循现有风格，即使你自己会采用不同写法。
- 如果发现与当前任务无关的死代码，可以指出，但不要删除。

当你的修改造成孤立或无用代码时：

- 删除因你本次修改而变得未使用的 import、变量和函数。
- 除非用户明确要求，不要删除修改前就存在的死代码。

检验标准：每一行改动都应能直接追溯到用户的请求。

### 4. 以目标驱动执行

**定义成功标准，循环执行直到验证通过。**

将任务转化为可验证的目标：

- “添加校验” → “先为无效输入编写测试，再使测试通过”。
- “修复缺陷” → “先编写能够复现缺陷的测试，再使测试通过”。
- “重构 X” → “确保重构前后的测试都能通过”。

对于多步骤任务，先给出简短计划：

```text
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

明确而有力的成功标准能让你独立循环完成任务。模糊的标准（如“让它能用”）则会导致不断需要澄清。

**准则生效的标志：** 差异中不必要的改动更少，因过度复杂而返工的次数更少，并且澄清问题发生在实现之前而不是出错之后。

## 实验过程中的文档要求

- 目录职责：`src/prefix_ttt/` 放模型、算子和可复用项目实现；`scripts/experiments/<project>/` 放实验启动、测量与分析工具；`docs/experiments/` 只放文档、图表及简要结果汇总；原始日志、trace、队列配置/状态、源码快照与测试报告放 `artifacts/experiments/<project>/`。不得在实验文档目录存放 Python/Shell 脚本、源码树或测试副本。图片仍按下述规则归档在文档的 `images/`。
- 在进行论文复现的时候，需要撰写的科研文档和教学文档使用中文，保持专业、连贯、深入浅出。
- Paper reading 必须讲清研究问题、动机、方法推导、实验、局限和可研究方向，明确区分论文声称与我们的分析。
- 教学文档按教材章节拆分，通过问题、直觉、例子和公式形成连续叙事，不写成术语解释列表。
- 每个长程实验首次启动时，创建稳定项目目录
  `docs/experiments/YYYY-MM-DD-<experiment-project>/`；其中日期是启动日期，
  `<experiment-project>` 是清晰、稳定的项目名。实验跨日继续时，必须持续更新该目录中的同一主账本、
  教学文档和总结，禁止再按自然日拆分出新的实验目录；只有启动新的实验项目时才创建新的目录。
- 合并旧规则下按自然日散落的同一项目文档时，先确认其项目归属，再移动而非复制到该稳定目录；保留原文中的
  日期小节，并在主账本记录旧路径、合并日期和新位置。仅在确认不含其他项目内容后，才移除旧的空目录。
- 实验文档必须严格区分“当前代码支持”“本阶段实际执行”和“已删除”。未在本阶段执行的策略、任务或
  配置不得表述为已删除；从本阶段报告、队列或教学结论中移除，也不等于工程能力已删除。声称删除时必须明确
  删除对象及其证据。
- 完成整个实验阶段后另写实验总结，重点说明做了什么、成功与失败、失败原因、已尝试方案、最终结果和结论。总结中少放命令；完整命令保留在配置和原始日志中。
- 文档需要引用图片时，必须将实际引用的图片复制到该文档所在目录的 `images/` 子目录，并使用相对路径（如
  `images/example.png`）引用该副本；不得依赖 `artifacts/` 或其他未随文档提交的路径。负责生成或更新文档的脚本也必须同步维护这份图片归档。

### Markdown 数学公式规范

- 行内数学统一使用 `$...$`；块级数学统一使用各自独占一行的 `$$` 开始和结束，公式内容写在两者之间。
- 禁止在需要渲染的公式中使用当前 Markdown 渲染器不支持的 `\(...\)` 或 `\[...\]` 定界符。反引号或代码块中的语法示例不属于公式，可按原样保留。
- 普通文本中的字面美元符号应写成 `\$` 或放入代码格式，避免被误识别为数学定界符；句末标点原则上放在数学定界符之外。
- `\frac`、`\sum`、`\mathrm` 等 LaTeX 命令必须位于 `$...$` 或 `$$...$$` 内；文档完成后必须检查定界符配对，并在排除行内代码和 fenced code block 后确认没有旧定界符或裸露 LaTeX 命令残留。含公式的表格、列表和图片标题也要纳入检查。

## 实验账本规则

- 每次代码修改、配置修改、任务提交、实验完成或失败后，立即更新当前实验项目目录中的主账本。
- 账本至少记录：目标、当前状态、完成项、运行项、失败项、关键决策、产物位置和下一步。
- OOM、磁盘写满、权重或数据缺失、网络失败、CUDA/依赖不兼容等必须如实记录。
- 对代码缺陷或明显配置错误可以进行一次有依据的修复与验证。
- 对需要用户处理的资源问题，保留日志并停止相关任务；不得反复重试、擅自清理用户数据或迁移大型文件。

## Python 与依赖

- 使用 `uv` 管理 Python 版本、依赖、锁文件和命令，正式命令统一通过 `uv run` 执行。
- 普通 Python 包优先使用清华 PyPI 镜像；PyTorch 使用与服务器驱动兼容的官方 PyTorch index，避免误装 CPU 版本。
- 一般不修改系统 CUDA Toolkit。编译 CUDA 扩展时，才通过 `CUDA_HOME`、`PATH`、`LD_LIBRARY_PATH` 选择服务器已有版本。
- Hugging Face 下载可设置 `HF_ENDPOINT=https://hf-mirror.com`，缓存必须共用，避免重复权重占满磁盘。
- 下载数据集、模型权重或其他大型公开文件时，优先查找并使用固定 repo、revision 和文件路径的
  Hugging Face 镜像；国内环境优先通过 `https://hf-mirror.com` 传输。镜像只作为传输渠道，原项目、
  论文或数据集官网仍是来源身份与许可依据，不得把第三方镜像记作官方发布者。
- 使用镜像前必须证明它与原始文件是同一字节对象：至少核对精确字节数和可信 SHA256；若原站没有
  SHA256，则固定镜像的 LFS SHA256，并在原文件或已有前缀可得时比较多个确定性 Range。跨来源断点
  续传前必须验证相同偏移的字节一致，下载完成后必须计算完整文件 SHA256；不一致时保留证据、停止
  使用，不得提升为正式产物。无法证明同一字节时回退原始来源，禁止用内容近似或重新打包的数据集
  静默替代。下载 URL、固定 revision、原始来源 URL、字节数、SHA256 和验证结果必须进入 acquisition
  manifest 或实验账本。

## Shell 与运行环境

- Ubuntu/Debian 默认 `.bashrc` 会在 shell flags 不含 `i` 时提前 `return`；它主要服务交互式 shell。不得把 `source ~/.bashrc` 作为脚本、Codex 命令、CI 或正式实验的环境初始化方式，也不要为了自动化任务删除这一默认保护逻辑。
- Codex 或其他终端自动化工具的临时命令可能由非交互式 `bash -lc` 解释；其中可以使用管道或重定向，但可执行文件发现、CUDA 选择和正式任务环境不得依赖 `.bashrc`。登录 shell 可能通过 `.profile` 获得部分 PATH，也不能据此推断 systemd 拥有相同环境。
- systemd 的 `ExecStart=` 和队列消费者的 `subprocess.Popen(argv, shell=False)` 属于无 shell 直接启动，不是“非交互 shell”；它们不会读取 `.bashrc` 或 `.profile`。必须继续使用 argv 数组，禁止为加载环境而改成 `shell=True`、`bash -c` 或 `eval`。
- 正式 systemd 服务应使用 `uv` 的绝对路径，并通过 `Environment=` 或受控任务环境显式设置 `CUDA_VISIBLE_DEVICES`、`CUDA_HOME`、`PATH`、`LD_LIBRARY_PATH`、`HF_ENDPOINT` 和队列根目录。交互终端中的便利配置可以留在 `.bashrc`，但不得成为实验可复现性的前提。

## GPU 与任务调度脚本

- 用户于2026-09-17明确：后续优化与性能实验不使用 CUDA Graph；已有 Graph 实验仅保留为历史记录。
- 用户于2026-09-17将首要性能目标改为 decode，并授权使用当前空闲的物理 GPU；启动前核对占用，允许的 GPU ID 显式传给调度器，同卡最多一个任务，性能对照在同一卡内完成。
- 优化方向先由实际执行路径的测量提出，再用单变量消融及普通 eager 完整层/整模型收益验证；算子耗时和理论流量估算不能代替端到端收益。生产实现优先替换、合并已有路径，避免为候选不断叠加形状特判和分派。
- 本项目只允许使用用户指定的空闲物理 GPU。如果有多个物理GPU，将允许使用的GPU配置为脚本参数。
- 对于简单的小型实验，你可以自行管理调度，这样比较简单。
- 正式的大型实验必须交给项目任务调度器；默认每张 GPU 同时最多运行一个任务，此值由配置控制。
- 任务必须以 argv 数组描述 `uv run ...`，不得使用 `eval` 或 `shell=True`。
- 正式调度器必须独立于 Codex 和终端生命周期。用户于2026-09-10批准当前容器使用 pixi 安装的 Supervisor 托管现有 SQLite 调度器，替代用户级 systemd；systemd 样例保留作参考。使用绝对 uv 路径、显式环境；消费者不自动重启，异常后先核对 SQLite 与遗留进程，防止重复训练。Supervisor 不提供跨独立进程组的 cgroup 清理或容器重建后自动恢复保证。
- 默认不自动重试失败任务；恢复运行时跳过已经成功的任务。

## 多智能体协作

- 优先把互相独立的阅读、文档、实现和审计任务交给 subagent。
- 可并行且边界清晰时应尽量使用可用的 subagent；subagent 必须避免文件冲突、重复实验和无边界的上下文扩散。
- 每个 subagent 必须拥有明确且互斥的文件或只读研究范围。
- 主智能体负责接口决策、结果集成、验证、账本和最终总结。
- subagent 不得各自手动启动 GPU benchmark；实验统一由任务调度器提交。

## Git 提交

- 只有在用户明确要求"提交"或"commit"时，才能创建 Git commit；完成修改、验证通过或工作树准备就绪都不构成提交授权。
- 获得明确提交授权后，必须使用 Git 身份 `Codex <codex@openai.com>`。

## Git 与远程仓库

- **推送必须获得用户当轮的明确授权**（如"推送到 GitHub"）。完成修改、清理、文档更新或账本记录都不构成推送理由，禁止顺手推送；获得一次授权只对当轮有效，不延续到后续修改。
- 未获授权时，不得创建、删除或改写远端分支，也不得使用 `git push --force` 或 `--force-with-lease` 重写远端历史。
- 获授权推送前先确认本地与远端分支状态；确需改写历史时优先使用 `--force-with-lease`，并在推送后报告远端引用变化。

## 验证要求

- 修改后运行与风险相匹配的单元测试、静态检查或 smoke test。
- 调度器至少验证 GPU 限制、单卡并发、环境传递、成功/失败/超时/中断/恢复，以及当前托管方式（本容器为 Supervisor）脱离终端后的存活性。
- 没有配置、日志和状态文件支撑的实验结果不得写入最终结论。
